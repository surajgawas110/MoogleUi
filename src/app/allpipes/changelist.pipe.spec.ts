import { ChangelistPipe } from './changelist.pipe';

describe('ChangelistPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangelistPipe();
    expect(pipe).toBeTruthy();
  });
});
